class Solution:
    def minimumDeletions(self, s: str) -> int:
        # all_a    — мин. удалений, чтобы обработанный префикс был чистыми 'a'
        # balanced — мин. удалений, чтобы префикс был сбалансирован (a..ab..b)
        all_a = balanced = 0

        for ch in s:  # обычный обход слева направо
            if ch == "a":
                # all_a:    'a' к чистому префиксу прилипает бесплатно
                # balanced: либо удалить эту 'a' (balanced + 1),
                #           либо префикс был ещё чистым — оставить бесплатно (all_a)
                all_a, balanced = all_a, min(balanced + 1, all_a)
            else:  # 'b'
                # all_a:    чтобы префикс остался чистым, 'b' придётся удалить
                # balanced: 'b' бесплатно дописывается к любому сбалансированному
                all_a, balanced = all_a + 1, min(all_a, balanced)

        # весь префикс обработан; ответ — сбалансированный вариант
        return balanced


"""
Разберём это решение подробно.

---

### Идея

Нам нужно получить строку вида `a* b*` (сначала все `a`, потом все `b`).  
Мы проходим по строке слева направо и поддерживаем **два состояния**:

1. **`all_a`** — минимальное число удалений, чтобы **весь обработанный префикс** состоял **только из `a`** (т.е. мы удалили все встреченные `b`).  
2. **`balanced`** — минимальное число удалений, чтобы **префикс был уже сбалансирован** (т.е. имел вид `a* b*`).

Оба значения пересчитываются для каждого нового символа.

---

### Как обновляются состояния

#### Если встретили `'a'`

- **Для `all_a`**  
  Добавление `'a'` к префиксу из одних `a` не требует удалений → `all_a` остаётся прежним.

- **Для `balanced`**  
  У нас есть два варианта:
  1. **Удалить эту `'a'`** — тогда стоимость = `balanced` + 1 (предыдущий сбалансированный префикс + удаление текущей буквы).  
  2. **Оставить эту `'a'`** — но тогда предыдущий префикс **не должен содержать `b`**, иначе появится пара `b...a`. Значит, мы должны взять префикс, который стал полностью `a` (это как раз `all_a`). Тогда текущая `'a'` просто дописывается в конец, удалений не добавляется.

  Выбираем минимум:  
  `balanced = min(balanced + 1, all_a)`

#### Если встретили `'b'`

- **Для `all_a`**  
  Чтобы префикс остался из одних `a`, эту `'b'` надо удалить → `all_a` увеличивается на 1.

- **Для `balanced`**  
  Добавление `'b'` к любому сбалансированному префиксу **не нарушает** баланс, поэтому мы можем просто взять предыдущий `balanced` и ничего не удалять.  
  Также мы можем взять префикс из одних `a` (`all_a`) и дописать к нему эту `'b'` — тоже получится сбалансированный префикс.  
  Удалять текущую `'b'` **не имеет смысла**, потому что оставление её даёт не больше удалений, чем удаление (удаление добавляет 1, а оставление — 0).  
  Поэтому:  
  `balanced = min(previous_balanced, previous_all_a)`

---

### Важное замечание по коду

В коде используется **одновременное присваивание**:

```python
all_a, balanced = all_a + 1, min(all_a, balanced)   # для 'b'
```

Здесь значения справа вычисляются **до** присваивания, поэтому `all_a` и `balanced` в правой части — это **старые** значения. Это эквивалентно описанным выше формулам.

Аналогично для `'a'`:

```python
all_a, balanced = all_a, min(balanced + 1, all_a)
```

---

### Почему это работает

Мы рассматриваем все возможные способы превратить префикс в допустимый.  
Переходы учитывают как удаление текущего символа, так и «переключение» между состояниями (удаление всех ранее встреченных `b`, чтобы сделать префикс чистым из `a`).  
В конце, когда обработана вся строка, `balanced` содержит минимальное число удалений для всей строки.

---

### Сложность

- Время: **O(n)** — один проход по строке.
- Память: **O(1)** — только две переменные.

---

### Пример

Для `s = "aababbab"`:

По шагам (начальные `all_a = 0, balanced = 0`):

- `'a'`: all_a = 0, balanced = min(0+1, 0) = 0  
- `'a'`: all_a = 0, balanced = min(0+1, 0) = 0  
- `'b'`: all_a = 1, balanced = min(0, 0) = 0  
- `'a'`: all_a = 1, balanced = min(0+1, 1) = 1  
- `'b'`: all_a = 2, balanced = min(1, 1) = 1  
- `'b'`: all_a = 3, balanced = min(1, 1) = 1  
- `'a'`: all_a = 3, balanced = min(1+1, 3) = 2  
- `'b'`: all_a = 4, balanced = min(2, 3) = 2  

Ответ: **2** — совпадает с примером.

---

Это элегантное решение, которое использует динамическое программирование с двумя состояниями и работает очень быстро.
"""
