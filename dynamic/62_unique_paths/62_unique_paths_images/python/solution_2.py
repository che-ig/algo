from math import comb


class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        # C(m+n-2, m-1): способы расставить (m-1) шагов Down
        # среди (m+n-2) шагов всего
        return comb(m + n - 2, m - 1)


"""
# Математическое решение: путь — это «слово» из шагов

## Шаг 1. У каждого пути одинаковый состав шагов

Робот стартует в `(0, 0)` и финиширует в `(m−1, n−1)`. Шаг **D** (down) увеличивает строку на 1, шаг **R** (right) — столбец на 1. Ходить назад нельзя, других ходов нет. Значит:

- чтобы поднять строку с 0 до m−1, нужно **ровно m−1 шагов D**;
- чтобы довести столбец до n−1, нужно **ровно n−1 шагов R**.

Итого каждый путь состоит из `(m−1) + (n−1) = m+n−2` шагов — длина у всех путей одинаковая, различается только **порядок** шагов.

## Шаг 2. Путь ↔ последовательность из D и R (биекция)

Каждому пути соответствует строка из `(m−1)` букв D и `(n−1)` букв R, и наоборот: любая такая строка — корректный путь (робот не вылетает за сетку, ведь в каждом префиксе не больше m−1 «вниз» и не больше n−1 «вправо»).

Пример `m=3, n=2` — ровно 2 буквы D и 1 буква R:

```
DDR  →  Down, Down, Right
DRD  →  Down, Right, Down
RDD  →  Right, Down, Down
```

— в точности три пути из условия задачи.

## Шаг 3. Посчитать последовательности = выбрать позиции

Представь `m+n−2` пустых слотов — позиции шагов в последовательности. Чтобы задать последовательность, достаточно **выбрать, в каких слотах стоят D**: оставшиеся слоты автоматически заполнятся R. Число способов выбрать `k` позиций из `N` — биномиальный коэффициент:

```
C(N, k) = N! / (k! · (N−k)!)
```

Подставляем `N = m+n−2`, `k = m−1`:

```
ответ = C(m+n−2, m−1)
```

(Можно выбирать позиции для R: `C(m+n−2, n−1)` — по симметрии это то же число.)

**Проверки:**
- `m=3, n=2`: C(3, 2) = 3 ✅
- `m=3, n=7`: C(8, 2) = 8·7 / 2 = 28 ✅

## Почему это согласуется с DP

Заполни DP-таблицу `paths[i][j] = paths[i-1][j] + paths[i][j-1]` с единицами по краям — получится **треугольник Паскаля**:

```
1  1  1  1  1  1  1
1  2  3  4  5  6  7
1  3  6 10 15 21 28
```

Рекуррентность путей — это в точности рекуррентность биномиальных коэффициентов `C(N,k) = C(N−1,k−1) + C(N−1,k)`. То есть формула `C(m+n−2, m−1)` — это **замкнутый вид** той же самой DP: математическое решение не «другое», оно аналитически решает ту же рекуррентность, поэтому и совпадает с ней во всех клетках.

**Вычислительно:** `math.comb` считает точное значение за O(min(m, n)) умножений — быстрее и короче любого DP, и без проблем с переполнением в Python.
"""
